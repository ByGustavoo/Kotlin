fun main() {

    // Inicializa a variável para armazenar a soma
    var soma = 0

    //Loop de 1 até 300
    for (i in 1..300)
        soma += i

    //Mostra o resultado
    println("\nA soma dos números no intervalo de 1 até 300 é: $soma")
}